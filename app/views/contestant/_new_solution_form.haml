.create-solution-form{"id" => problem.id}
  = form_for([@competition, problem, Solution.new] ) do |f|
    = f.text_area :source_code, :class => "source_code"
    %br
    = submit_tag "Submit solution"
